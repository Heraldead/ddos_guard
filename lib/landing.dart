import 'package:flutter/material.dart';

class KandingPage extends StatelessWidget {
  const KandingPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text('');
  }
}
